/**
 * SaleRequest
 * Contains the branchId to retrieve all sales for that branch.
 */
export interface SaleRequest {

  /**
   * The branchId to retrieve all sales for that branch.
   */
  branchId: string;
}
